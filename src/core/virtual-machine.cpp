#include "virtual-machine.h"

void sigil::virtual_machine::console_subprogram() {
    status_t status = VM_OK;

    // while (status == sigil::VM_OK) {
    //     printf("sigil -> ");
    //     fflush(stdout);
    //     std::string payload;
    //     std::cin >> payload;
    //     status = exe(payload);
    //     printf("\n");
    // }
}